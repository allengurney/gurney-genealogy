# data/indexes/

Generated lookup indexes for canonical IDs in `data/*.json`. Regenerate with `.\.venv\Scripts\python.exe tools\generate_id_indexes.py --write`; check freshness with `.\.venv\Scripts\python.exe tools\generate_id_indexes.py --check`.

Use these indexes for ID discovery before opening large canonical JSON files.
